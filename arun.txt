pipeline {
    agent any
    stages {
        stage('stage_1') {
            when {
                expression {
                    env.BRANCH_NAME == 'main'
                }   
            }
            steps {
                echo "branch exists"
            }
        }
     }
}
